# Module 3 Quiz — APIs, HTTP and Async Python

## 1. What is the strongest reason to use explicit contracts in this module?
A. They make agent behavior testable and bounded
B. They make Python run without an interpreter
C. They eliminate every model error
D. They remove the need for security

## 2. Which approach is safest for production agent actions?
A. Let the model execute arbitrary code
B. Validate inputs and enforce permissions before execution
C. Store credentials in prompts
D. Retry forever

## 3. What should a good test suite include?
A. Only happy paths
B. Only model quality scores
C. Success and failure scenarios
D. Screenshots only

## 4. Why separate probabilistic decisions from deterministic execution?
A. To make side effects governable and testable
B. To increase prompt length
C. To avoid Python functions
D. To eliminate APIs

## 5. What is a useful debugging artifact?
A. A trace showing state, decisions and tool results
B. A hidden exception
C. A longer system prompt only
D. A random retry loop

## 6. When should an agent request human approval?
A. For every token
B. Before a high-risk or irreversible action
C. Never
D. Only after the action fails

## 7. What is least privilege?
A. Giving every tool admin rights
B. Granting only the permissions required for the task
C. Removing authentication
D. Sharing one secret across tools

## 8. What makes an exercise portfolio-ready?
A. A working happy path only
B. README, tests, architecture, evaluation and failure handling
C. A notebook with no explanation
D. A screenshot of code
